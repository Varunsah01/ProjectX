# Local Expo development only.
# Copy this to packages/mobile-app/.env.local for local testing.
# Internal and production-like builds must inject this at build time.
EXPO_PUBLIC_API_URL=https://your-mobile-api.example.com

# Optional: enables extra device-test logging in JS for debug/internal validation.
EXPO_PUBLIC_ENABLE_TEST_LOGS=false
