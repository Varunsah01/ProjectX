# Branding TODO

"Project X" is a placeholder name. When final branding is decided, update:

- [ ] `SITE_CONFIG.name` in `packages/landing/lib/constants.ts`
- [ ] Regenerate `packages/landing/public/logo.svg` and `logo-mark.svg` with final wordmark
- [ ] Landing `app/layout.tsx` metadata (title, OG, schema.org)
- [ ] Landing `app/page.tsx` metadata
- [ ] `app/opengraph-image.tsx` text
- [ ] Web-app login page branding, email templates, sidebar logo
- [ ] Domain references in seed data (`app.projectx.in` → final domain)
